function t = e_03_my_sum_type (dt)
    switch dt 
        case 'double'
            t.x = double([]);
            t.y = double([]);
        case 'single'
            t.x = single([]);
            t.y = single([]);   
    end    
end 